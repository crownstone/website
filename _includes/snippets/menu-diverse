<li class="nav-item dropdown">
    <a class="nav-link dropdown-toggle" href="#" id="navbarDropdownMenuLink" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
        {% if site.active_lang == "nl" %}
        Waar te koop
        {% else %}
        Where to buy
        {% endif %}
    </a>
    <div class="dropdown-menu" aria-labelledby="navbarDropdownMenuLink" style="z-index: 1001; padding: 0;">
    <a class="dropdown-item {{node.markup}}" href="https://shop.crownstone.rocks?launch={{ site.active_lang }}&ref=http://crownstone.rocks/{{ site.active_lang}}{{ page.url }}" style="background-color: transparent; color: #ff8400">
        <i class="el el-shopping-cart" style="margin-right: 0.2rem"></i>
        {% if site.active_lang == "nl" %}
        Winkel
        {% else %}
        Shop
        {% endif %}
    </a>
    <a class="dropdown-item inactive {{node.markup}}" href="https://subscribe.crownstone.rocks/welcome?ref=http://crownstone.rocks/{{ site.active_lang}}{{ page.url }}" style="background-color: transparent; color: #ff8400">
            <i class="el el-user" style="margin-right: 0.2rem"></i>
            {% if site.active_lang == "nl" %}
            Abonnement
            {% else %}
            Subscription
            {% endif %}
        </a>
    </div>
</li>
{% for tongue in site.languages %}
<li class="nav-item">
	{% assign nav-url = page.url | prepend:site.baseurl | prepend:site.url %}
	{% if tongue != site.default_lang %}
	{% assign nav-url = page.url | prepend:tongue | prepend:'/' | prepend:site.baseurl | prepend:site.url %}
	{% endif %}
	<!-- note the space before nav-url, without it polyglot will relativize it: https://github.com/untra/polyglot -->
	<a {% if tongue == site.active_lang %}style="font-weight: bold;"{% endif %} href=" {{ nav-url }}" class="nav-link lang-menu" style="border-bottom: 2px solid white">{{ tongue }}</a>
</li>
{% endfor %}